var Map = require('../map')
var Value = require('../value')

var value = Value('blah')
var map = Map({cat: 'meow'})
map(x => console.log(x))

map.put('dog', 'woof')
map.put('sheep', 'baa')
map.put('cow', 'moo')
map.put('human', value)
map.delete('sheep')
console.log('keys =>', map.keys())
map.set({ cat: 'meow', dog: 'woof', sheep: 'baa', human: value })
map.clear()
